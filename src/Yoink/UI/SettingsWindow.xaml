<Window x:Class="Yoink.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Yoink"
        Height="580" Width="540"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Foreground="White"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        Icon="pack://application:,,,/yoink.ico">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"
                      ResizeBorderThickness="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="ThemeTextPrimaryBrush" Color="#FFF0F0F0"/>
        <SolidColorBrush x:Key="ThemeTextSecondaryBrush" Color="#FFA0A0A0"/>
        <SolidColorBrush x:Key="ThemeMutedBrush" Color="#FF7A7A7A"/>
        <SolidColorBrush x:Key="ThemeCardBrush" Color="#FF252525"/>
        <SolidColorBrush x:Key="ThemeTabActiveBrush" Color="#FF2A2A2A"/>
        <SolidColorBrush x:Key="ThemeTabHoverBrush" Color="#14000000"/>
        <SolidColorBrush x:Key="ThemeInputBackgroundBrush" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="ThemeInputBorderBrush" Color="#28FFFFFF"/>
        <SolidColorBrush x:Key="ThemeWindowBorderBrush" Color="#15FFFFFF"/>
        <SolidColorBrush x:Key="ThemeAccentBrush" Color="#FFFFFFFF"/>

        <Style x:Key="TabPill" TargetType="RadioButton">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Padding="14,6" CornerRadius="6" Cursor="Hand" Background="Transparent">
                            <TextBlock x:Name="Tb" Text="{TemplateBinding Content}" FontSize="12"
                                       Foreground="{DynamicResource ThemeTextPrimaryBrush}" Opacity="0.4"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="1"/>
                                <Setter TargetName="Tb" Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SubTab" TargetType="RadioButton">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Padding="10,4" CornerRadius="5" Cursor="Hand" Background="Transparent">
                            <TextBlock x:Name="Tb" Text="{TemplateBinding Content}" FontSize="11" Foreground="{DynamicResource ThemeTextPrimaryBrush}" Opacity="0.35"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHoverBrush}"/>
                                <Setter TargetName="Tb" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tb" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Opacity" Value="0.35"/>
            <Setter Property="Margin" Value="4,0,0,6"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>

        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="Background" Value="{DynamicResource ThemeCardBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeWindowBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Dark theme textbox for hotkeys -->
        <Style x:Key="HotkeyBox" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="115"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Background" Value="{DynamicResource ThemeInputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ThemeInputBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ThemeTextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small clear button -->
        <Style x:Key="ClearBtn" TargetType="Button">
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeTextSecondaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" CornerRadius="5" Background="{DynamicResource ThemeTabHoverBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabActiveBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

        <Border Margin="16" CornerRadius="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="6" Opacity="0.5" Color="Black"/>
        </Border.Effect>
    <Border x:Name="OuterBorder" CornerRadius="12" ClipToBounds="True" Background="#1C1C1C"
            BorderBrush="#15FFFFFF" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom title bar -->
        <Border x:Name="TitleBarBorder" Grid.Row="0" Padding="14,10,8,10" Background="#161616"
                CornerRadius="11,11,0,0"
                MouseLeftButtonDown="TitleBar_MouseDown">
            <Grid>
                <TextBlock x:Name="TitleText" Text="Yoink" FontSize="12" FontWeight="SemiBold" Foreground="White"
                           Opacity="0.8" VerticalAlignment="Center"/>
                <Border Width="32" Height="28" CornerRadius="6" Cursor="Hand" Background="Transparent"
                        MouseLeftButtonDown="CloseBtn_Click" MouseEnter="TitleBtn_Enter" MouseLeave="TitleBtn_Leave"
                        HorizontalAlignment="Right">
                    <Canvas Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Line X1="0" Y1="0" X2="10" Y2="10" Stroke="#AAFFFFFF" StrokeThickness="1.5"/>
                        <Line X1="10" Y1="0" X2="0" Y2="10" Stroke="#AAFFFFFF" StrokeThickness="1.5"/>
                    </Canvas>
                </Border>
            </Grid>
        </Border>

        <!-- Main tabs -->
        <Border Grid.Row="1" Padding="16,6,16,6">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="SettingsTab" Content="Settings" GroupName="Main"
                             IsChecked="True" Click="TabChanged" Style="{StaticResource TabPill}"/>
                <RadioButton x:Name="HistoryTab" Content="History" GroupName="Main"
                             Click="TabChanged" Margin="2,0,0,0" Style="{StaticResource TabPill}"/>
                <RadioButton x:Name="UploadsTab" Content="Uploads" GroupName="Main"
                             Click="TabChanged" Margin="2,0,0,0" Style="{StaticResource TabPill}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">

            <!-- Settings -->
            <ScrollViewer x:Name="SettingsPanel" VerticalScrollBarVisibility="Auto" Padding="18,4,18,18">
                <StackPanel>
                    <TextBlock Text="HOTKEYS" Style="{StaticResource SectionLabel}"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <!-- Screenshot hotkey -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Screenshot" FontSize="13"/>
                                    <TextBlock Text="You can use PrintScreen or try Win+Shift+S here too" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="HotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="HotkeyBox_PreviewKeyDown"
                                             GotFocus="HotkeyBox_GotFocus" LostFocus="HotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearCaptureHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <!-- OCR hotkey -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Text capture (OCR)" FontSize="13"/>
                                    <TextBlock Text="Copies text from screen" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="OcrHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="OcrHotkeyBox_PreviewKeyDown"
                                             GotFocus="OcrHotkeyBox_GotFocus" LostFocus="OcrHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearOcrHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <!-- Color picker hotkey -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Color picker" FontSize="13"/>
                                    <TextBlock Text="Pick color, copies hex" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="PickerHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="PickerHotkeyBox_PreviewKeyDown"
                                             GotFocus="PickerHotkeyBox_GotFocus" LostFocus="PickerHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearPickerHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <!-- Scanner hotkey -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="QR/barcode scanner" FontSize="13"/>
                                    <TextBlock Text="Optional" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="ScanHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="ScanHotkeyBox_PreviewKeyDown"
                                             GotFocus="ScanHotkeyBox_GotFocus" LostFocus="ScanHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearScanHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <!-- Sticker hotkey -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Sticker" FontSize="13"/>
                                    <TextBlock Text="Optional" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="StickerHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="StickerHotkeyBox_PreviewKeyDown"
                                             GotFocus="StickerHotkeyBox_GotFocus" LostFocus="StickerHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearStickerHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Fullscreen capture" FontSize="13"/>
                                    <TextBlock Text="Captures the whole virtual screen immediately" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="FullscreenHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="FullscreenHotkeyBox_PreviewKeyDown"
                                             GotFocus="FullscreenHotkeyBox_GotFocus" LostFocus="FullscreenHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearFullscreenHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Active window capture" FontSize="13"/>
                                    <TextBlock Text="Captures the focused window immediately" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="ActiveWindowHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="ActiveWindowHotkeyBox_PreviewKeyDown"
                                             GotFocus="ActiveWindowHotkeyBox_GotFocus" LostFocus="ActiveWindowHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearActiveWindowHotkey_Click"/>
                                </StackPanel>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <!-- GIF recording hotkey -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Record GIF" FontSize="13"/>
                                    <TextBlock Text="Optional" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <TextBox x:Name="GifHotkeyBox" Style="{StaticResource HotkeyBox}"
                                             PreviewKeyDown="GifHotkeyBox_PreviewKeyDown"
                                             GotFocus="GifHotkeyBox_GotFocus" LostFocus="GifHotkeyBox_LostFocus"/>
                                    <Button Content="X" Style="{StaticResource ClearBtn}" Click="ClearGifHotkey_Click"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="CAPTURE" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>

                    <Border Style="{StaticResource Card}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Default capture tool" FontSize="13"/>
                                <TextBlock Text="What the main screenshot hotkey opens" FontSize="11" Opacity="0.25"/>
                            </StackPanel>
                            <ComboBox x:Name="DefaultCaptureModeCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="145"
                                      SelectionChanged="DefaultCaptureModeCombo_SelectionChanged">
                                <ComboBoxItem Content="Rectangle"/>
                                <ComboBoxItem Content="Freeform"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Window detection" FontSize="13"/>
                                <TextBlock Text="Snaps to windows when you hover the capture area" FontSize="11" Opacity="0.25" TextWrapping="Wrap" Width="250"/>
                            </StackPanel>
                            <ComboBox x:Name="WindowDetectionCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="145"
                                      SelectionChanged="WindowDetectionCombo_SelectionChanged">
                                <ComboBoxItem Content="Off"/>
                                <ComboBoxItem Content="Windows only"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="After capture" FontSize="13" VerticalAlignment="Center"/>
                            <ComboBox x:Name="AfterCaptureCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="145"
                                      SelectionChanged="AfterCaptureCombo_SelectionChanged">
                                <ComboBoxItem Content="Copy to clipboard"/>
                                <ComboBoxItem Content="Show preview"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <TextBlock Text="SAVING" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <CheckBox x:Name="SaveToFileCheck" Content="Save screenshots to file" FontSize="13"
                                      Checked="SaveToFileCheck_Changed" Unchecked="SaveToFileCheck_Changed"/>
                            <CheckBox x:Name="AskFileNameCheck" Content="Ask for file name every time" FontSize="13" Margin="0,8,0,0"
                                      Checked="AskFileNameCheck_Changed" Unchecked="AskFileNameCheck_Changed"/>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Default format" FontSize="12" Opacity="0.6"/>
                                    <TextBlock Text="PNG keeps detail, JPG is smaller, BMP is fastest" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <ComboBox x:Name="CaptureFormatCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="120"
                                          SelectionChanged="CaptureFormatCombo_SelectionChanged">
                                    <ComboBoxItem Content="PNG"/>
                                    <ComboBoxItem Content="JPG"/>
                                    <ComboBoxItem Content="BMP"/>
                                </ComboBox>
                            </Grid>
                            <Grid x:Name="JpegQualityPanel" Margin="0,10,0,0" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="JPG quality" FontSize="12" Opacity="0.6"/>
                                    <TextBlock Text="Higher quality keeps more detail but makes bigger files" FontSize="11" Opacity="0.25" TextWrapping="Wrap" Width="250"/>
                                </StackPanel>
                                <ComboBox x:Name="JpegQualityCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="120"
                                          SelectionChanged="JpegQualityCombo_SelectionChanged">
                                    <ComboBoxItem Content="100 - Best" Tag="100"/>
                                    <ComboBoxItem Content="90 - High" Tag="90"/>
                                    <ComboBoxItem Content="85 - Balanced" Tag="85"/>
                                    <ComboBoxItem Content="75 - Smaller" Tag="75"/>
                                    <ComboBoxItem Content="60 - Tiny" Tag="60"/>
                                </ComboBox>
                            </Grid>
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Max image size" FontSize="12" Opacity="0.6"/>
                                    <TextBlock Text="Scales the longest edge before saving, previewing, copying, and uploading" FontSize="11" Opacity="0.25" TextWrapping="Wrap" Width="250"/>
                                </StackPanel>
                                <ComboBox x:Name="CaptureSizeCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="120"
                                          SelectionChanged="CaptureSizeCombo_SelectionChanged">
                                    <ComboBoxItem Content="Original" Tag="0"/>
                                    <ComboBoxItem Content="2160p" Tag="2160"/>
                                    <ComboBoxItem Content="1440p" Tag="1440"/>
                                    <ComboBoxItem Content="1080p" Tag="1080"/>
                                    <ComboBoxItem Content="720p" Tag="720"/>
                                    <ComboBoxItem Content="480p" Tag="480"/>
                                </ComboBox>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0" x:Name="SaveDirPanel" Visibility="Collapsed">
                                <TextBox x:Name="SaveDirBox" IsReadOnly="True" FontSize="11" Padding="7,4" Width="280"
                                          Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <Button Content="Browse" FontSize="11" Padding="8,4" Margin="5,0,0,0" Click="BrowseButton_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <CheckBox x:Name="SaveHistoryCheck" Content="Save capture history" FontSize="13"
                                      Checked="SaveHistoryCheck_Changed" Unchecked="SaveHistoryCheck_Changed"/>

                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Auto-clear" FontSize="12" Opacity="0.6"/>
                                </StackPanel>
                                <ComboBox x:Name="HistoryRetentionCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="120"
                                          SelectionChanged="HistoryRetentionCombo_SelectionChanged">
                                    <ComboBoxItem Content="Never"/>
                                    <ComboBoxItem Content="1 day"/>
                                    <ComboBoxItem Content="7 days"/>
                                    <ComboBoxItem Content="30 days"/>
                                    <ComboBoxItem Content="3 months"/>
                                </ComboBox>
                            </Grid>
                            <TextBlock Text="History now follows the same image format, size, and JPG quality settings above." FontSize="11" Opacity="0.25" Margin="0,8,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="TOOLBAR" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <TextBlock Text="Choose which tools appear in the capture toolbar" FontSize="12" Opacity="0.35" Margin="0,0,0,8"/>
                            <CheckBox x:Name="ShowToolNumberBadgesCheck" Content="Show numbered tool badges on hover" FontSize="12"
                                      Margin="0,0,0,8" Checked="ShowToolNumberBadgesCheck_Changed" Unchecked="ShowToolNumberBadgesCheck_Changed"/>
                            <WrapPanel x:Name="ToolTogglePanel" Orientation="Horizontal"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="GENERAL" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <CheckBox x:Name="MuteSoundsCheck" Content="Mute capture sounds" FontSize="13"
                                      Checked="MuteSoundsCheck_Changed" Unchecked="MuteSoundsCheck_Changed"/>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <CheckBox x:Name="CrosshairGuidesCheck" Content="Show crosshair guides" FontSize="13"
                                      Checked="CrosshairGuidesCheck_Changed" Unchecked="CrosshairGuidesCheck_Changed"/>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <CheckBox x:Name="DetectWindowsCheck" Content="Auto-detect windows" FontSize="13"
                                      Checked="DetectWindowsCheck_Changed" Unchecked="DetectWindowsCheck_Changed"/>
                            <TextBlock Text="Snap selection to visible windows on hover" FontSize="11" Opacity="0.25" Margin="0,2,0,0"/>

                            <CheckBox x:Name="DetectControlsCheck" Content="Auto-detect UI controls" FontSize="13" Margin="0,8,0,0"
                                      Checked="DetectControlsCheck_Changed" Unchecked="DetectControlsCheck_Changed"/>
                            <TextBlock Text="Also detect buttons, panels, and other UI elements within windows" FontSize="11" Opacity="0.25" Margin="0,2,0,0"/>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Toast position" FontSize="13"/>
                                    <TextBlock Text="Where notifications appear" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <ComboBox x:Name="ToastPositionCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="125"
                                          SelectionChanged="ToastPositionCombo_SelectionChanged">
                                    <ComboBoxItem Content="Right"/>
                                    <ComboBoxItem Content="Left"/>
                                    <ComboBoxItem Content="Top Left"/>
                                    <ComboBoxItem Content="Top Right"/>
                                </ComboBox>
                            </Grid>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <CheckBox x:Name="StartWithWindowsCheck" Content="Launch on startup" FontSize="13"
                                      Checked="StartWithWindowsCheck_Changed" Unchecked="StartWithWindowsCheck_Changed"/>

                            <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                            <Button Content="Uninstall Yoink" FontSize="12" Padding="10,6"
                                    HorizontalAlignment="Left" Click="UninstallButton_Click"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="UPDATES" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <CheckBox x:Name="AutoUpdateCheck" Content="Automatically check GitHub releases on startup" FontSize="13"
                                      Checked="AutoUpdateCheck_Changed" Unchecked="AutoUpdateCheck_Changed"/>

                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="UpdateStatusText" Text="Checking for updates..." FontSize="12.5" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="UpdateDetailText" Text="GitHub releases power update checks and downloads." FontSize="11" Opacity="0.35" TextWrapping="Wrap" Margin="0,2,8,0"/>
                                </StackPanel>
                                <Button x:Name="CheckUpdatesButton" Grid.Column="1" Content="Check now" FontSize="11" Padding="10,5" Margin="8,0,0,0" Click="CheckUpdatesButton_Click"/>
                                <Button x:Name="DownloadUpdateButton" Grid.Column="2" Content="Open release" FontSize="11" Padding="10,5" Margin="6,0,0,0" Visibility="Collapsed" Click="DownloadUpdateButton_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- About -->
                    <TextBlock x:Name="VersionText" FontSize="10.5" Opacity="0.22" HorizontalAlignment="Center" Margin="0,18,0,2">
                        <Run Text="Yoink"/>
                    </TextBlock>
                    <TextBlock HorizontalAlignment="Center" Margin="0,0,0,4">
                        <Hyperlink NavigateUri="https://github.com/jasperdevs/yoink" RequestNavigate="Hyperlink_Navigate"
                                   Foreground="{DynamicResource ThemeAccentBrush}" FontSize="10.5">GitHub</Hyperlink>
                    </TextBlock>
                </StackPanel>
            </ScrollViewer>

            <!-- History (merged: Screenshots + Text sub-tabs) -->
            <Grid x:Name="HistoryPanel" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Sub-tabs -->
                <Border Grid.Row="0" Padding="14,4,14,2">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="ImagesSubTab" Content="Images" GroupName="HistSub"
                                         IsChecked="True" Click="HistorySubTabChanged" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="TextSubTab" Content="Text" GroupName="HistSub"
                                         Click="HistorySubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="GifsSubTab" Content="GIFs" GroupName="HistSub"
                                         Click="HistorySubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="ColorsSubTab" Content="Colors" GroupName="HistSub"
                                         Click="HistorySubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="StickersSubTab" Content="Stickers" GroupName="HistSub"
                                         Click="HistorySubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="SelectBtn" Content="Select" FontSize="10" Padding="8,3"
                                     Margin="0,0,3,0" Click="ToggleSelectMode"/>
                            <Button x:Name="DeleteAllBtn" Content="Delete All" FontSize="10" Padding="8,3"
                                    Margin="0,0,3,0" Click="DeleteAllClick"/>
                            <Button x:Name="DeleteSelectedBtn" Content="Delete" FontSize="10"
                                     Padding="8,3" Visibility="Collapsed" Click="DeleteSelectedClick"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Images history -->
                <ScrollViewer x:Name="ImagesPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10,2,10,4"
                              ScrollChanged="HistoryPanel_ScrollChanged">
                    <StackPanel x:Name="HistoryStack"/>
                </ScrollViewer>

                <!-- GIF history -->
                <ScrollViewer x:Name="GifsPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10,2,10,4"
                              ScrollChanged="GifPanel_ScrollChanged"
                              Visibility="Collapsed">
                    <StackPanel x:Name="GifStack"/>
                </ScrollViewer>

                <!-- Text history -->
                <ScrollViewer x:Name="TextPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="14,2,14,14" Visibility="Collapsed">
                    <StackPanel x:Name="OcrStack"/>
                </ScrollViewer>

                <!-- Color history -->
                <ScrollViewer x:Name="ColorsPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="14,2,14,14" Visibility="Collapsed">
                    <WrapPanel x:Name="ColorStack"/>
                </ScrollViewer>

                <!-- Sticker history -->
                <ScrollViewer x:Name="StickersPanel" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                              Padding="10,2,10,4" Visibility="Collapsed"
                              ScrollChanged="StickerPanel_ScrollChanged">
                    <StackPanel x:Name="StickerStack"/>
                </ScrollViewer>

                <!-- Empty states -->
                <StackPanel x:Name="HistoryEmptyText" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="&#x1F4F7;" FontSize="32" HorizontalAlignment="Center" Opacity="0.15"/>
                    <TextBlock x:Name="HistoryEmptyLabel" Text="No captures yet" FontSize="13" Opacity="0.2"
                               HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <!-- Bottom bar -->
                <Border Grid.Row="2" Padding="14,4,14,8">
                    <TextBlock x:Name="HistoryCountText" FontSize="11" Opacity="0.25" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <!-- Uploads -->
            <ScrollViewer x:Name="UploadsPanel" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" Padding="18,4,18,18">
                <StackPanel>
                    <Border Padding="0,0,0,10">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="UploadImagesSubTab" Content="Images" GroupName="UploadSub"
                                         IsChecked="True" Click="UploadSubTabChanged" Style="{StaticResource SubTab}"/>
                            <RadioButton x:Name="UploadStickersSubTab" Content="Stickers" GroupName="UploadSub"
                                         Click="UploadSubTabChanged" Margin="2,0,0,0" Style="{StaticResource SubTab}"/>
                        </StackPanel>
                    </Border>

                    <StackPanel x:Name="ImageUploadsPanel">
                    <TextBlock Text="DESTINATION" Style="{StaticResource SectionLabel}"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Upload service" FontSize="13"/>
                                    <TextBlock Text="Where captures are uploaded" FontSize="11" Opacity="0.25"/>
                                </StackPanel>
                                <ComboBox x:Name="UploadDestCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="145"
                                          SelectionChanged="UploadDestCombo_Changed">
                                    <ComboBoxItem Content="None"/>
                                    <ComboBoxItem Content="Imgur"/>
                                    <ComboBoxItem Content="ImgBB"/>
                                    <ComboBoxItem Content="Catbox"/>
                                    <ComboBoxItem Content="Litterbox (temp)"/>
                                    <ComboBoxItem Content="Gyazo"/>
                                    <ComboBoxItem Content="file.io"/>
                                    <ComboBoxItem Content="Uguu (temp)"/>
                                    <ComboBoxItem Content="transfer.sh"/>
                                    <ComboBoxItem Content="Dropbox"/>
                                    <ComboBoxItem Content="Google Drive"/>
                                    <ComboBoxItem Content="OneDrive"/>
                                    <ComboBoxItem Content="Azure Blob"/>
                                    <ComboBoxItem Content="GitHub"/>
                                    <ComboBoxItem Content="Immich"/>
                                    <ComboBoxItem Content="FTP"/>
                                    <ComboBoxItem Content="SFTP"/>
                                    <ComboBoxItem Content="WebDAV"/>
                                    <ComboBoxItem Content="S3 / R2 / B2"/>
                                    <ComboBoxItem Content="Custom HTTP"/>
                                </ComboBox>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Imgur settings -->
                    <StackPanel x:Name="ImgurSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/imgur_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="IMGUR" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="PNG" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="JPG" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="GIF" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <TextBlock Text="  20MB images · 200MB GIF · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="1,250 uploads/day anonymous · 12,500/day with account" FontSize="10" Opacity="0.25" Margin="0,0,0,8"/>
                                <TextBlock Text="Anonymous uploads work without any configuration. Add your own credentials for account uploads." FontSize="11" Opacity="0.35" Margin="0,0,0,8" TextWrapping="Wrap"/>

                                <TextBlock Text="Client ID" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBlock Text="Optional — has built-in default for anonymous uploads" FontSize="11" Opacity="0.25" Margin="0,0,0,4"/>
                                <TextBox x:Name="ImgurClientIdBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="ImgurClientIdBox_Changed"/>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBlock Text="Optional — for uploading to your Imgur account" FontSize="11" Opacity="0.25" Margin="0,0,0,4"/>
                                <TextBox x:Name="ImgurTokenBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="ImgurTokenBox_Changed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- ImgBB settings -->
                    <StackPanel x:Name="ImgBBSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/imgbb_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="IMGBB" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="PNG" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="JPG" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="GIF" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <TextBlock Text="  32MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="100 uploads/hour on free tier" FontSize="10" Opacity="0.25" Margin="0,0,0,8"/>

                                <TextBlock Text="API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBlock FontSize="11" Opacity="0.25" Margin="0,0,0,4" TextWrapping="Wrap">
                                    <Run Text="Get a free API key at "/>
                                    <Hyperlink NavigateUri="https://api.imgbb.com/" RequestNavigate="Hyperlink_Navigate"
                                               Foreground="#1962F4" FontSize="11">api.imgbb.com</Hyperlink>
                                </TextBlock>
                                <TextBox x:Name="ImgBBKeyBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="ImgBBKeyBox_Changed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Catbox info -->
                    <StackPanel x:Name="CatboxSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/catbox_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="CATBOX" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <TextBlock Text="  200MB max · Free · Permanent" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="No rate limits · No account required · No configuration needed" FontSize="10" Opacity="0.25" Margin="0,0,0,4"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Litterbox info -->
                    <StackPanel x:Name="LitterboxSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/litterbox_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="LITTERBOX" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#30EF4444" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Expires 72h" FontSize="9" Foreground="#EF4444"/>
                                    </Border>
                                    <TextBlock Text="  200MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="No rate limits · No account required · No configuration needed" FontSize="10" Opacity="0.25" Margin="0,0,0,4"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Gyazo settings -->
                    <StackPanel x:Name="GyazoSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/gyazo_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="GYAZO" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="PNG" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="JPG" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="GIF" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <TextBlock Text="  25MB max · Free tier + Pro $4/mo" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GyazoTokenBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="GyazoTokenBox_Changed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- file.io info -->
                    <StackPanel x:Name="FileIoSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/fileio_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="FILE.IO" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#30EF4444" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="1 download then deleted" FontSize="9" Foreground="#EF4444"/>
                                    </Border>
                                    <TextBlock Text="  100MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="No configuration needed. Files auto-delete after first download." FontSize="10" Opacity="0.25" Margin="0,0,0,4"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Uguu info -->
                    <StackPanel x:Name="UguuSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/uguu_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="UGUU" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#30EF4444" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Expires 48h" FontSize="9" Foreground="#EF4444"/>
                                    </Border>
                                    <TextBlock Text="  128MB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="No configuration needed. No account required." FontSize="10" Opacity="0.25" Margin="0,0,0,4"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- S3-Compatible settings -->
                    <StackPanel x:Name="S3Settings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/aws_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="S3 / R2 / B2" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <TextBlock Text="  Works with AWS S3, Cloudflare R2, Backblaze B2, MinIO, etc." FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>

                                <TextBlock Text="Endpoint URL" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBlock Text="e.g. https://s3.us-east-1.amazonaws.com or https://your-id.r2.cloudflarestorage.com" FontSize="10" Opacity="0.2" Margin="0,0,0,4" TextWrapping="Wrap"/>
                                <TextBox x:Name="S3EndpointBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="S3EndpointBox_Changed"/>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Bucket" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox x:Name="S3BucketBox" FontSize="11" Padding="7,4"
                                                 Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                                 TextChanged="S3BucketBox_Changed"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Region" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                        <TextBox x:Name="S3RegionBox" FontSize="11" Padding="7,4" Text="auto"
                                                 Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                                 TextChanged="S3RegionBox_Changed"/>
                                    </StackPanel>
                                </Grid>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <TextBlock Text="Access Key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="S3AccessKeyBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="S3AccessKeyBox_Changed"/>

                                <TextBlock Text="Secret Key" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="S3SecretKeyBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="S3SecretKeyBox_Changed"/>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <TextBlock Text="Public URL (optional)" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBlock Text="Custom domain for public links, e.g. https://cdn.example.com" FontSize="10" Opacity="0.2" Margin="0,0,0,4"/>
                                <TextBox x:Name="S3PublicUrlBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="S3PublicUrlBox_Changed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- transfer.sh info -->
                    <StackPanel x:Name="TransferSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/transfer_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="TRANSFER.SH" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Border CornerRadius="3" Background="#15FFFFFF" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Any file" FontSize="9" Opacity="0.6"/>
                                    </Border>
                                    <Border CornerRadius="3" Background="#30EF4444" Padding="5,2" Margin="0,0,4,0">
                                        <TextBlock Text="Expires 14d" FontSize="9" Foreground="#EF4444"/>
                                    </Border>
                                    <TextBlock Text="  10GB max · Free" FontSize="10" Opacity="0.3" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="No configuration needed. Public temporary file hosting." FontSize="10" Opacity="0.25" Margin="0,0,0,4"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="DropboxSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/dropbox_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="DROPBOX" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="DropboxTokenBox" FontSize="11" Padding="7,4" TextChanged="DropboxTokenBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Folder / path prefix" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="DropboxPathBox" FontSize="11" Padding="7,4" TextChanged="DropboxPathBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="GoogleDriveSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/gdrive_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="GOOGLE DRIVE" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="GoogleDriveTokenBox" FontSize="11" Padding="7,4" TextChanged="GoogleDriveTokenBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Folder ID (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GoogleDriveFolderBox" FontSize="11" Padding="7,4" TextChanged="GoogleDriveFolderBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="OneDriveSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/onedrive_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="ONEDRIVE" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Access token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="OneDriveTokenBox" FontSize="11" Padding="7,4" TextChanged="OneDriveTokenBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Folder" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="OneDriveFolderBox" FontSize="11" Padding="7,4" TextChanged="OneDriveFolderBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="AzureSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/aws_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="AZURE BLOB" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Container SAS URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="AzureBlobSasBox" FontSize="11" Padding="7,4" TextChanged="AzureBlobSasBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="GitHubSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/github_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="GITHUB" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Token" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="GitHubTokenBox" FontSize="11" Padding="7,4" TextChanged="GitHubTokenBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Repo (owner/name)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GitHubRepoBox" FontSize="11" Padding="7,4" TextChanged="GitHubRepoBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Branch" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="GitHubBranchBox" FontSize="11" Padding="7,4" TextChanged="GitHubBranchBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="ImmichSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="24" Height="24" CornerRadius="7" Background="#16FFFFFF" BorderBrush="#28FFFFFF" BorderThickness="1" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Image Source="pack://application:,,,/Assets/immich_sq.png" Width="16" Height="16" Stretch="Uniform"/>
                            </Border>
                            <TextBlock Text="IMMICH" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Base URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="ImmichUrlBox" FontSize="11" Padding="7,4" TextChanged="ImmichUrlBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="API key" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="ImmichApiKeyBox" FontSize="11" Padding="7,4" TextChanged="ImmichApiKeyBox_Changed"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="FtpSettings" Visibility="Collapsed">
                        <TextBlock Text="FTP" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="FTP URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="FtpUrlBox" FontSize="11" Padding="7,4" TextChanged="FtpUrlBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Username" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="FtpUsernameBox" FontSize="11" Padding="7,4" TextChanged="FtpUsernameBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Password" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="FtpPasswordBox" FontSize="11" Padding="7,4" TextChanged="FtpPasswordBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Public URL base (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="FtpPublicUrlBox" FontSize="11" Padding="7,4" TextChanged="FtpPublicUrlBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="SftpSettings" Visibility="Collapsed">
                        <TextBlock Text="SFTP" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Host" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="SftpHostBox" FontSize="11" Padding="7,4" TextChanged="SftpHostBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Port" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpPortBox" FontSize="11" Padding="7,4" TextChanged="SftpPortBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Username" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpUsernameBox" FontSize="11" Padding="7,4" TextChanged="SftpUsernameBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Password" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpPasswordBox" FontSize="11" Padding="7,4" TextChanged="SftpPasswordBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Remote path" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpRemotePathBox" FontSize="11" Padding="7,4" TextChanged="SftpRemotePathBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Public URL base (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="SftpPublicUrlBox" FontSize="11" Padding="7,4" TextChanged="SftpPublicUrlBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel x:Name="WebDavSettings" Visibility="Collapsed">
                        <TextBlock Text="WEBDAV" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="WebDAV URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="WebDavUrlBox" FontSize="11" Padding="7,4" TextChanged="WebDavUrlBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Username" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="WebDavUsernameBox" FontSize="11" Padding="7,4" TextChanged="WebDavUsernameBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Password" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="WebDavPasswordBox" FontSize="11" Padding="7,4" TextChanged="WebDavPasswordBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                                <TextBlock Text="Public URL base (optional)" FontSize="12" Opacity="0.6" Margin="0,8,0,4"/>
                                <TextBox x:Name="WebDavPublicUrlBox" FontSize="11" Padding="7,4" TextChanged="WebDavPublicUrlBox_Changed" Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Custom HTTP settings -->
                    <StackPanel x:Name="CustomUploadSettings" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Margin="4,16,0,6">
                            <Border Width="16" Height="16" CornerRadius="3" Background="#5865F2" Margin="0,0,6,0" VerticalAlignment="Center">
                                <TextBlock Text="&#x2B06;" FontSize="9" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                            </Border>
                            <TextBlock Text="CUSTOM HTTP" Style="{StaticResource SectionLabel}" Margin="0"/>
                        </StackPanel>
                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Text="Upload URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="CustomUrlBox" FontSize="11" Padding="7,4"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="CustomUrlBox_Changed"/>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <TextBlock Text="File form field name" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBox x:Name="CustomFieldBox" FontSize="11" Padding="7,4" Text="file"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="CustomFieldBox_Changed"/>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <TextBlock Text="Response JSON path to URL" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBlock Text="Dot-separated path, e.g. data.url or just url" FontSize="11" Opacity="0.25" Margin="0,0,0,4"/>
                                <TextBox x:Name="CustomJsonPathBox" FontSize="11" Padding="7,4" Text="url"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="CustomJsonPathBox_Changed"/>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <TextBlock Text="Custom headers" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                <TextBlock Text="One per line, format: Name: Value" FontSize="11" Opacity="0.25" Margin="0,0,0,4"/>
                                <TextBox x:Name="CustomHeadersBox" FontSize="11" Padding="7,4" AcceptsReturn="True"
                                         Height="70" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                         Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                         TextChanged="CustomHeadersBox_Changed"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <TextBlock Text="AUTO-UPLOAD" Style="{StaticResource SectionLabel}" Margin="4,16,0,6"/>

                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <CheckBox x:Name="AutoUploadScreenshotsCheck" Content="Auto-upload screenshots" FontSize="13"
                                      Checked="AutoUploadScreenshotsCheck_Changed" Unchecked="AutoUploadScreenshotsCheck_Changed"/>
                            <TextBlock Text="Uploads after every screenshot capture and attaches the link to the preview/history item" FontSize="11" Opacity="0.25" Margin="0,2,0,0" TextWrapping="Wrap"/>

                        </StackPanel>
                    </Border>

                    <!-- Test upload -->
                    <Border Style="{StaticResource Card}" Margin="0,14,0,0" x:Name="TestUploadCard" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="Test your upload configuration by sending a tiny 1x1 pixel image." FontSize="11" Opacity="0.35" Margin="0,0,0,8"/>
                            <Button x:Name="TestUploadBtn" Content="Test Upload" FontSize="12" Padding="12,6"
                                    HorizontalAlignment="Left" Click="TestUpload_Click" Cursor="Hand"/>
                        </StackPanel>
                    </Border>
                    </StackPanel>

                    <StackPanel x:Name="StickerUploadsPanel" Visibility="Collapsed">
                        <TextBlock Text="STICKER" Style="{StaticResource SectionLabel}"/>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="Sticker provider" FontSize="13"/>
                                        <TextBlock Text="Removes the background before saving, previewing, and copying" FontSize="11" Opacity="0.25" TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <ComboBox x:Name="StickerProviderCombo" Grid.Column="1" FontSize="12" Padding="8,5" MinWidth="145"
                                              SelectionChanged="StickerProviderCombo_Changed">
                                        <ComboBoxItem Content="None"/>
                                        <ComboBoxItem Content="Remove.bg"/>
                                        <ComboBoxItem Content="Photoroom"/>
                                        <ComboBoxItem Content="Local CPU"/>
                                    </ComboBox>
                                </Grid>

                                <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                <StackPanel x:Name="StickerRemoveBgPanel" Visibility="Collapsed">
                                    <TextBlock Text="remove.bg API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                    <TextBox x:Name="StickerRemoveBgKeyBox" FontSize="11" Padding="7,4"
                                             Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                             TextChanged="StickerRemoveBgKeyBox_Changed"/>
                                </StackPanel>

                                <StackPanel x:Name="StickerPhotoroomPanel" Visibility="Collapsed">
                                    <TextBlock Text="Photoroom API key" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                    <TextBox x:Name="StickerPhotoroomKeyBox" FontSize="11" Padding="7,4"
                                             Background="#1A1A1A" Foreground="#99FFFFFF" BorderBrush="#18FFFFFF" BorderThickness="1"
                                             TextChanged="StickerPhotoroomKeyBox_Changed"/>
                                </StackPanel>

                                <StackPanel x:Name="StickerLocalCpuPanel" Visibility="Collapsed">
                                    <TextBlock Text="Local engine" FontSize="12" Opacity="0.6" Margin="0,0,0,4"/>
                                    <TextBlock Text="This runs on your PC only. Yoink downloads an open-source model and removes the background in-app, so nothing leaves your machine." FontSize="11" Opacity="0.25" Margin="0,0,0,8" TextWrapping="Wrap"/>

                                    <ComboBox x:Name="StickerLocalEngineCombo" FontSize="12" Padding="8,5" MinWidth="180"
                                              SelectionChanged="StickerLocalEngineCombo_Changed">
                                        <ComboBoxItem Content="BRIA RMBG"/>
                                        <ComboBoxItem Content="U2Netp"/>
                                    </ComboBox>

                                    <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                    <TextBlock x:Name="StickerLocalEngineStatusText" Text="Pick a local engine." FontSize="11" Opacity="0.35" TextWrapping="Wrap"/>
                                    <ProgressBar x:Name="StickerLocalEngineProgress" Height="6" Margin="0,8,0,0" Minimum="0" Maximum="100" Visibility="Collapsed"/>
                                    <TextBlock x:Name="StickerLocalEngineProgressText" FontSize="10" Opacity="0.25" Margin="0,4,0,0" Visibility="Collapsed"/>

                                    <Border Height="1" Background="#0AFFFFFF" Margin="0,10,0,10"/>

                                    <CheckBox x:Name="StickerShadowCheck" Content="Add drop shadow" FontSize="13"
                                              Checked="StickerShadowCheck_Changed" Unchecked="StickerShadowCheck_Changed"/>
                                    <CheckBox x:Name="StickerStrokeCheck" Content="Add white stroke" FontSize="13" Margin="0,8,0,0"
                                              Checked="StickerStrokeCheck_Changed" Unchecked="StickerStrokeCheck_Changed"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button x:Name="StickerDownloadRembgBtn" Content="Download from GitHub" FontSize="12" Padding="12,6"
                                                Click="StickerDownloadRembgBtn_Click" Cursor="Hand"/>
                                        <Button x:Name="StickerOpenLocalEngineRepoBtn" Content="Open GitHub" FontSize="12" Padding="12,6" Margin="8,0,0,0"
                                                Click="StickerOpenLocalEngineRepoBtn_Click" Cursor="Hand"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
    </Border>
    </Border>
</Window>
