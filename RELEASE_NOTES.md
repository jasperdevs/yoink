# Yoink v0.5.2.8

## Highlights
- Fixed standalone exe downloads crashing on launch due to missing native WPF libraries.
- Fixed the update flow failing to relaunch Yoink after applying an update.
- Fixed the installer not appearing when running a downloaded release exe.
- The app now shows a message instead of silently exiting when another instance is already running.
