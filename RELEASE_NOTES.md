# Yoink v0.5.2.7

## Highlights
- Fixed the installer not appearing when running a downloaded release exe.
- Fixed the update flow failing to relaunch Yoink — the helper now waits for the old process to fully exit before applying the update and relaunching.
- The app now shows a message instead of silently exiting when another instance is already running.
