# Yoink v0.8.24

## Fixed
- fix Velopack GitHub update source URL so in-app updates can download release packages.

## Notes
- v0.8.23 users need to install this release manually because the v0.8.23 updater cannot parse its update source URL.
